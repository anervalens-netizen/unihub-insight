{
  "name": "unihub-insight",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop retail intelligence for the UniHub ecosystem.",
  "workspaces": [
    "apps/web"
  ],
  "engines": {
    "node": ">=24.17 <25",
    "npm": ">=11 <12"
  },
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "dev:web": "npm --workspace @unihub-insight/web run dev",
    "dev:api": "uv run --project apps/api uvicorn unihub_insight_api.main:app --reload --host 0.0.0.0 --port 8100",
    "lint": "npm --workspace @unihub-insight/web run lint && uv run --project apps/api ruff check apps/api/src apps/api/tests",
    "format": "npm --workspace @unihub-insight/web run format && uv run --project apps/api ruff format apps/api/src apps/api/tests",
    "format:check": "npm --workspace @unihub-insight/web run format:check && uv run --project apps/api ruff format --check apps/api/src apps/api/tests",
    "typecheck": "npm --workspace @unihub-insight/web run typecheck && uv run --project apps/api mypy --config-file apps/api/pyproject.toml apps/api/src",
    "test": "npm --workspace @unihub-insight/web run test && uv run --project apps/api pytest apps/api/tests",
    "build": "npm --workspace @unihub-insight/web run build",
    "bundle:check": "node scripts/check-bundle.mjs",
    "verify": "bash scripts/verify.sh"
  }
}
